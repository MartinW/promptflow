// @promptflow/core — shared CRUD logic, Langfuse wrapper, tag conventions.
// Day 2 fills this in. For now, just an export stub so the workspace resolves.

export const PROMPTFLOW_CORE_VERSION = "0.0.0";
